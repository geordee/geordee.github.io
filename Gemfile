source 'https://rubygems.org'

gem 'jekyll', '~> 4.3'

group :jekyll_plugins do
  gem 'jekyll-paginate', '~> 1.1'
end

# Required for `jekyll serve` on Ruby 3+ (no longer in the standard library)
gem 'webrick', '~> 1.8'
